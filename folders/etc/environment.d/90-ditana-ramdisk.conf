# Expose the RAM-backed scratch directory to systemd user services and
# logind-started sessions. The directory itself is created by
# systemd-tmpfiles (see /usr/share/user-tmpfiles.d/ditana-ramdisk.conf).
RAMDISK=${XDG_RUNTIME_DIR}/ramdisk
