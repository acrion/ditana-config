# Cap ZFS ARC size. The OpenZFS default (all RAM minus 1 GiB) targets
# dedicated storage servers; on desktop systems it competes with
# application memory and ZRAM. Value computed at install time.
options zfs zfs_arc_max=@@ZFS_ARC_MAX@@
